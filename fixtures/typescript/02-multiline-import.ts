import {
    itemsPerPage,
    itemInfo,
} from '../../lib/shop/constants';
import * from 'react';
import {
    a,
    b,
    c // not sure if we need that!
} from './abc'

export function ItemsPage() {
    return <ItemsView />
}