// Conforms. 'UserProfile' is the canonical PascalCase spelling of
// 'user-profile', so the allows clause exempts this import.
import { helper } from '@/internal/UserProfile/helper';

export const userProfile = helper;
