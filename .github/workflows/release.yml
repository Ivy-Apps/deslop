name: Release to NPM

on:
  push:
    tags:
      - 'v*'

jobs:
  build-linux:
    name: Build (${{ matrix.npm-platform }})
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            npm-platform: linux-x64
          - os: ubuntu-24.04-arm
            npm-platform: linux-arm64
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v6

      - name: Cache cabal store
        uses: actions/cache@v5
        with:
          path: ~/.cabal/store
          key: cabal-${{ matrix.npm-platform }}-${{ hashFiles('cabal.project.freeze') }}
          restore-keys: cabal-${{ matrix.npm-platform }}-

      - name: Build static binary
        run: |
          docker run --rm \
            -v "${{ github.workspace }}:/workspace" \
            -v "$HOME/.cabal/store:/root/.cabal/store" \
            -w /workspace \
            alpine:edge \
            sh -c "
              apk add --no-cache \
                ghc cabal build-base linux-headers \
                zlib-dev zlib-static \
                xz-dev \
                ncurses-dev ncurses-static \
                gmp-dev \
                libffi-dev \
                openssl-dev openssl-libs-static \
                ca-certificates \
                curl &&
              cabal update &&
              cabal build exe:deslop \
                --enable-executable-static \
                --ghc-option=-optl-static \
                --ghc-option=-O2 \
                --ghc-option=-threaded \
                --ghc-option=-rtsopts \
                --ghc-option=-with-rtsopts=-N &&
              mkdir -p /workspace/dist &&
              cp \$(cabal list-bin exe:deslop) /workspace/dist/deslop &&
              strip --strip-unneeded /workspace/dist/deslop
            "

      - name: Collect binary
        run: |
          mkdir -p dist/${{ matrix.npm-platform }}
          mv dist/deslop dist/${{ matrix.npm-platform }}/deslop
          strip --strip-unneeded dist/${{ matrix.npm-platform }}/deslop

      - uses: actions/upload-artifact@v4
        with:
          name: bin-${{ matrix.npm-platform }}
          path: dist/${{ matrix.npm-platform }}

  build-macos:
    name: Build (darwin-arm64)
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v6

      - uses: haskell-actions/setup@v2
        with:
          ghc-version: '9.10.3'
          cabal-version: '3.16.0.0'

      - name: Cache cabal store
        uses: actions/cache@v4
        with:
          path: ~/.cabal/store
          key: cabal-darwin-arm64-${{ hashFiles('cabal.project.freeze') }}
          restore-keys: cabal-darwin-arm64-

      - run: cabal update

      - name: Build binary
        run: |
          cabal build exe:deslop \
            --ghc-option=-O2 \
            --ghc-option=-optP-Wno-nonportable-include-path \
            --ghc-option=-threaded \
            --ghc-option=-rtsopts \
            --ghc-option=-with-rtsopts=-N

      - name: Collect binary
        run: |
          mkdir -p dist/darwin-arm64
          cp $(cabal list-bin exe:deslop) dist/darwin-arm64/deslop
          strip -x dist/darwin-arm64/deslop

      - uses: actions/upload-artifact@v4
        with:
          name: bin-darwin-arm64
          path: dist/darwin-arm64

  publish-npm:
    name: Publish to NPM
    needs: [build-linux, build-macos]
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          registry-url: 'https://registry.npmjs.org'
          scope: '@ivy-apps'

      - run: npm install -g npm@latest

      - uses: actions/download-artifact@v4
        with:
          path: dist

      - name: Prepare version
        run: |
          TAG=${{ github.ref_name }}
          echo "VERSION=${TAG#v}" >> $GITHUB_ENV

      - name: Publish platform packages
        run: |
          for PLATFORM in darwin-arm64 linux-arm64 linux-x64; do
            PKG_DIR="npm/packages/$PLATFORM"
            mkdir -p "$PKG_DIR/bin"
            cp -r "dist/bin-$PLATFORM/." "$PKG_DIR/bin/"
            chmod +x "$PKG_DIR/bin/deslop"
            cd "$PKG_DIR"
            npm version "$VERSION" --no-git-tag-version --allow-same-version
            npm publish --access public
            cd "$GITHUB_WORKSPACE"
          done

      - name: Publish main package
        run: |
          cd npm
          npm version "$VERSION" --no-git-tag-version --allow-same-version

          # Pin optionalDependencies to the exact release version
          npm pkg set optionalDependencies.@ivy-apps/deslop-darwin-arm64="$VERSION"
          npm pkg set optionalDependencies.@ivy-apps/deslop-linux-arm64="$VERSION"
          npm pkg set optionalDependencies.@ivy-apps/deslop-linux-x64="$VERSION"

          npm publish --access public
