[Title] 🚀 Checking project: ~/fixtures/ts-monorepo-project
[Warning] WARNING: No architecture rules loaded. Deslop is only running its built-in checks.
Define your own rules in deslop/rules/*.yaml - see https://deslop.dev
[Error] Found 2 problems:
[Error] ─────────────────────────────────────────
[Error] [AUTO-FIXABLE] # no-relative-imports#~/fixtures/ts-monorepo-project/packages/app/src/checkout.ts
Relative imports are not allowed. Use aliased ones.
```ts
import { Button } from '../../ui/src/Button';
```
FIX: Use ```import { Button } from '@ui/Button';``` instead.
---------

[AUTO-FIXABLE] # no-relative-imports#~/fixtures/ts-monorepo-project/packages/app/src/main.ts
Relative imports are not allowed. Use aliased ones.
```ts
import { formatMoney } from './helpers/format';
```
FIX: Use ```import { formatMoney } from '@app/helpers/format';``` instead.
[Error] ─────────────────────────────────────────
[summary] Checked 4 modules enforcing 2 rules
[exit] Found 2 problems, 2 of them auto-fixable.
   Run `deslop fix` to fix the 2 auto-fixable problems.
   Run `deslop baseline` to silence all 2 problems.
