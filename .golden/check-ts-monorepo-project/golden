[Title] 🚀 Checking project: ts-monorepo-project
[Plain] 📚 Loaded 1 rulebook, 1 rule
[Error] Found 3 problems:
[Error] ─────────────────────────────────────────
[Error] [AUTO-FIXABLE] # no-relative-imports#packages/app/src/checkout.ts
Relative imports are not allowed. Use aliased ones.
```ts
import { Button } from '../../ui/src/Button';
```
FIX: Use ```import { Button } from '@ui/Button';``` instead.
---------

[AUTO-FIXABLE] # no-relative-imports#packages/app/src/main.ts
Relative imports are not allowed. Use aliased ones.
```ts
import { formatMoney } from './helpers/format';
```
FIX: Use ```import { formatMoney } from '@app/helpers/format';``` instead.
---------

# workspace-boundaries#scripts-keep-out-of-the-packages#/scripts/release
A release script wires the workspaces together; it must not depend on what any one of them renders.

Module '/scripts/release' directly imports '@ui/Button'.
```ts
import { Button } from '@ui/Button';
```
FIX: Move the shared code into a module both the script and the package can import, rather than reaching into the UI package from a script.
[Error] ─────────────────────────────────────────
[summary] Checked 5 modules enforcing 4 rules
[exit] Found 3 problems, 2 of them auto-fixable.
   Run `deslop fix` to fix the 2 auto-fixable problems.
   Run `deslop baseline` to silence all 3 problems.
