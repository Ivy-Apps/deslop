[Title] 🚀 Checking project: ~/test/fixtures/ts-globplus-project
[Plain] 📚 Loaded 1 rulebook, 12 rules
[Error] Found 17 problems:
[Error] ─────────────────────────────────────────
[Error] # provider-services#adapter-imports-its-provider-config#@/adapters/stripe-connect/stripe-connect-payment-gateway
An adapter names its provider twice: once as the folder it sits in, and once as the prefix of its own file name, before the service type. The greedy split of that file segment gives the provider one word too many, and the folder above has already ruled it out - so the split has to be chosen by what is already bound rather than merely checked against it.

Module '@/adapters/stripe-connect/stripe-connect-payment-gateway' must import '@/config/stripe-connect'.
FIX: Import @/config/stripe-connect, the config of the provider your folder names, rather than the payment-gateway adapter's own.
---------

# provider-services#nested-view-uses-its-provider-config#@/components/paypal/payment/RefundView
A View at any depth under a provider folder must import that provider's config. The provider variable sits before the **, so the group that captures it is numbered after the globstar's own - the arrangement in which reading captures by position binds the provider to the globstar.

Module '@/components/paypal/payment/RefundView' must import '@/config/paypal'.
FIX: Import @/config/paypal rather than another provider's config.
---------

# provider-services#nested-view-uses-its-shared-formatter#@/components/stripe-connect/payment/widgets/BadgeView
Every View imports the money formatter from the shared folder one directory back from its own. @/components/stripe-connect/payment/widgets is the directory of the matched FILE rather than of the rule, so this clause names a different module for a View nested one level deeper. That is documented behaviour rather than a bug: see "`..` is relative to the file, not to the rule".

Module '@/components/stripe-connect/payment/widgets/BadgeView' must import '@/components/stripe-connect/payment/shared/format-money'.
FIX: Import the format-money module from the shared folder beside your own, or move this View up to the depth where that folder is its sibling.
---------

# provider-services#nested-view-uses-its-shared-formatter#@/components/stripe-connect/payment/widgets/BadgeView
Every View imports the money formatter from the shared folder one directory back from its own. @/components/stripe-connect/payment/widgets is the directory of the matched FILE rather than of the rule, so this clause names a different module for a View nested one level deeper. That is documented behaviour rather than a bug: see "`..` is relative to the file, not to the rule".

Module '@/components/stripe-connect/payment/widgets/BadgeView' requires '@/components/stripe-connect/payment/shared/format-money' to exist.
FIX: Import the format-money module from the shared folder beside your own, or move this View up to the depth where that folder is its sibling.
---------

# provider-services#provider-entry-is-named-after-its-folder#@/components/square/SquareEntry
A provider's entry point carries its provider's name. The same variable occurs on both sides of the **, so the rule applies only where the folder and the file agree - and never applied at all while the globstar's text was being read as the provider.

Module '@/components/square/SquareEntry' requires '@/config/square' to exist.
FIX: Name the entry point after the provider folder it sits under.
---------

# provider-services#providers-are-isolated#@/components/paypal
One provider's components must never import another provider's components. Anything shared between providers belongs outside @/components.

Module '@/components/paypal' directly imports '@/components/stripe-connect/payment/CheckoutView'.
```ts
import { CheckoutView } from '@/components/stripe-connect/payment/CheckoutView';
```
FIX: Import only from within @/components/paypal, or promote the shared code out of the provider folders.
---------

# provider-services#providers-are-isolated#@/components/paypal/payment/RefundView
One provider's components must never import another provider's components. Anything shared between providers belongs outside @/components.

Module '@/components/paypal/payment/RefundView' directly imports '@/components/stripe-connect/payment/CheckoutView'.
```ts
import { CheckoutView } from '@/components/stripe-connect/payment/CheckoutView';
```
FIX: Import only from within @/components/paypal, or promote the shared code out of the provider folders.
---------

# provider-services#storybook-has-a-visual-test#@/components/stripe-connect/payment/CheckoutView.stories
Every story is paired with a visual test. This rule's own prose carries variables: Checkout and @/components/stripe-connect/payment are substituted from the match, while the misspelled {{FileNam}} names nothing and is printed as written.

Module '@/components/stripe-connect/payment/CheckoutView.stories' requires '@/components/stripe-connect/payment/CheckoutView.visual' to exist.
FIX: Add @/components/stripe-connect/payment/CheckoutView.visual next to the story for the stripe-connect provider. Not {{FileNam}}.
---------

# provider-services#use-case-has-a-view-model#@/application/notify-user/NotifyUserUseCase
Every use case is driven by a ViewModel named after it, in camelCase this time - the same three words spelled a third way.

Module '@/application/notify-user/NotifyUserUseCase' requires '@/application/notify-user/useNotifyUserViewModel' to exist.
FIX: Add a useNotifyUserViewModel next to the use case.
---------

# provider-services#view-has-a-storybook#@/components/paypal/payment/RefundView
Every service View must be documented in Storybook.

Module '@/components/paypal/payment/RefundView' requires '@/components/paypal/payment/RefundView.stories' to exist.
FIX: Add a RefundView.stories.tsx file next to the View.
---------

# provider-services#view-has-a-storybook#@/components/stripe-connect/payout/TransferView
Every service View must be documented in Storybook.

Module '@/components/stripe-connect/payout/TransferView' requires '@/components/stripe-connect/payout/TransferView.stories' to exist.
FIX: Add a TransferView.stories.tsx file next to the View.
---------

# provider-services#view-may-reach-its-providers-shared-folder#@/components/paypal/payment/RefundView
A View may use its own service folder and the provider's shared folder, which sits one directory back from @/components/paypal/payment. A sibling *service* folder of the same provider is not reachable that way, so the .. in this clause is what decides where the allowance stops - providers-are-isolated permits that import, and this rule does not.

Module '@/components/paypal/payment/RefundView' directly imports '@/components/stripe-connect/payment/CheckoutView'.
```ts
import { CheckoutView } from '@/components/stripe-connect/payment/CheckoutView';
```
FIX: Import from your own payment folder or from @/components/paypal/shared, and promote anything else there.
---------

# provider-services#view-may-reach-its-providers-shared-folder#@/components/stripe-connect/payout/TransferView
A View may use its own service folder and the provider's shared folder, which sits one directory back from @/components/stripe-connect/payout. A sibling *service* folder of the same provider is not reachable that way, so the .. in this clause is what decides where the allowance stops - providers-are-isolated permits that import, and this rule does not.

Module '@/components/stripe-connect/payout/TransferView' directly imports '@/components/stripe-connect/payment/CheckoutView'.
```ts
import { CheckoutView } from '@/components/stripe-connect/payment/CheckoutView';
```
FIX: Import from your own payout folder or from @/components/stripe-connect/shared, and promote anything else there.
---------

# provider-services#view-model-calls-its-own-provider-service#@/components/stripe-connect/payout/useTransferViewModel
A ViewModel may only talk to the service module of the provider and service type it belongs to. The service module name is derived from all three variables, so a ViewModel cannot reach into another provider's service.

Module '@/components/stripe-connect/payout/useTransferViewModel' must import '@/services/stripe-connect/payout-transfer'.
FIX: Import @/services/stripe-connect/payout-transfer rather than another provider's service module.
---------

# provider-services#view-model-drives-its-use-case#@/application/db-sync-job/useDBSyncJobViewModel
A use case's ViewModel drives that use case and no other. The variable is three words long and occurs three times - as the kebab-case folder, and in PascalCase inside two different affixed segments - so every word of it has to survive being written out in two casings.

Module '@/application/db-sync-job/useDBSyncJobViewModel' must import '@/application/db-sync-job/DBSyncJobUseCase'.
FIX: Import @/application/db-sync-job/DBSyncJobUseCase rather than another use case.
---------

# provider-services#view-model-drives-its-use-case#@/application/refund-payment/useRefundPaymentViewModel
A use case's ViewModel drives that use case and no other. The variable is three words long and occurs three times - as the kebab-case folder, and in PascalCase inside two different affixed segments - so every word of it has to survive being written out in two casings.

Module '@/application/refund-payment/useRefundPaymentViewModel' must import '@/application/refund-payment/RefundPaymentUseCase'.
FIX: Import @/application/refund-payment/RefundPaymentUseCase rather than another use case.
---------

# provider-services#view-wires-own-view-model#@/components/paypal/payment/RefundView
A service View is pure UI. It must delegate its state and behaviour to the ViewModel hook that sits next to it in the same provider/service folder.

Module '@/components/paypal/payment/RefundView' must import '@/components/paypal/payment/useRefundViewModel'.
FIX: Import useRefundViewModel from the same directory and drive the View from the state it returns.
[Error] ─────────────────────────────────────────
[summary] Checked 28 modules enforcing 14 rules
[exit] Found 17 problems, none auto-fixable.
   Run `deslop baseline` to silence all 17 problems.
