[Title] 🚀 Checking project: ts-barrel-project
[Plain] 📚 Loaded 1 rulebook, 3 rules
[Error] Found 3 problems:
[Error] ─────────────────────────────────────────
[Error] [AUTO-FIXABLE] # no-relative-exports#src/lib/index.ts
Relative re-exports are not allowed. Use aliased ones.
```ts
export * from "./helper";
```
FIX: Use ```export * from "@/lib/helper";``` instead.
---------

# barrels#app-keeps-out-of-server#@/app/explicit
No app module may reach the server layer, however many barrels the chain runs through. The chain is app -> the home barrel -> home-service -> helper -> server/db, so it is only found if re-exports are edges and the barrel's two spellings are one module.

Module '@/app/explicit' transitively imports '@/server/db' (4 hops) via: @/app/explicit → @/features/home/index → @/features/home/home-service → @/lib/helper → @/server/db.
```ts
import { greet } from "@/features/home/index";
```
FIX: Do not depend on @/server from the app layer, directly or through a barrel.
---------

# barrels#app-keeps-out-of-server#@/app/page
No app module may reach the server layer, however many barrels the chain runs through. The chain is app -> the home barrel -> home-service -> helper -> server/db, so it is only found if re-exports are edges and the barrel's two spellings are one module.

Module '@/app/page' transitively imports '@/server/db' (4 hops) via: @/app/page → @/features/home/index → @/features/home/home-service → @/lib/helper → @/server/db.
```ts
import { greet } from "@/features/home";
```
FIX: Do not depend on @/server from the app layer, directly or through a barrel.
[Error] ─────────────────────────────────────────
[summary] Checked 8 modules enforcing 6 rules
[exit] Found 3 problems, 1 of them auto-fixable.
   Run `deslop fix` to fix the 1 auto-fixable problem.
   Run `deslop baseline` to silence all 3 problems.
