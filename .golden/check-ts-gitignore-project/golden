[Title] 🚀 Checking project: ~/test/fixtures/ts-gitignore-project
[Warning] WARNING: No architecture rules loaded. Deslop is only running its built-in checks.
Define your own rules in deslop/rules/*.yaml - see https://deslop.dev
[Error] Found 4 problems:
[Error] ─────────────────────────────────────────
[Error] [AUTO-FIXABLE] # no-relative-imports#src/bad.ts
Relative imports are not allowed. Use aliased ones.
```ts
import { app } from './app';
```
FIX: Use ```import { app } from '@/app';``` instead.
---------

[AUTO-FIXABLE] # no-relative-imports#src/features/sub/scratch.ts
Relative imports are not allowed. Use aliased ones.
```ts
import { app } from '../../app';
```
FIX: Use ```import { app } from '@/app';``` instead.
---------

[AUTO-FIXABLE] # no-relative-imports#src/keep.gen.ts
Relative imports are not allowed. Use aliased ones.
```ts
import { app } from './app';
```
FIX: Use ```import { app } from '@/app';``` instead.
---------

[AUTO-FIXABLE] # no-relative-imports#src/other.gen.ts
Relative imports are not allowed. Use aliased ones.
```ts
import { app } from './app';
```
FIX: Use ```import { app } from '@/app';``` instead.
[Error] ─────────────────────────────────────────
[summary] Checked 5 modules enforcing 2 rules
[exit] Found 4 problems, 4 of them auto-fixable.
   Run `deslop fix` to fix the 4 auto-fixable problems.
   Run `deslop baseline` to silence all 4 problems.
