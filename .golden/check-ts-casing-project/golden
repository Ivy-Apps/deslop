[Title] 🚀 Checking project: ~/test/fixtures/ts-casing-project
[Plain] 📚 Loaded 1 rulebook, 3 rules
[Error] Found 4 problems:
[Error] ─────────────────────────────────────────
[Error] # casing#view-is-named-after-its-provider#@/components/db-pool/DBPoolView
A provider's View carries the provider's name. The same variable occurs as the kebab-case folder and as the PascalCase file, so it binds only where some one name spells both - which is what makes HTTPClient and http-client the same provider, and Paypal and stripe-connect not.

Module '@/components/db-pool/DBPoolView' must import '@/config/db-pool'.
FIX: Import @/config/{{provider-name}}, the config of the provider you sit under.
---------

# casing#widget-uses-its-config#@/widgets/AWSS3Widget
A Widget's config is named after it. The name is captured in PascalCase only, so kebab-case has to be derived, and a run of capitals is read as a single word.

Module '@/widgets/AWSS3Widget' must import '@/config/awss3'.
FIX: Import @/config/{{file-name}}.
---------

# casing#widget-uses-its-config#@/widgets/ArchiveDBOrderWidget
A Widget's config is named after it. The name is captured in PascalCase only, so kebab-case has to be derived, and a run of capitals is read as a single word.

Module '@/widgets/ArchiveDBOrderWidget' must import '@/config/archive-db-order'.
FIX: Import @/config/{{file-name}}.
---------

# casing#widgets-keep-out-of-internal#@/widgets/db-connection
A kebab-case widget must not reach into its own @/internal folder. The clause names the variable in PascalCase, which has more than one spelling; a forbidding clause accepts them all, because an import that slips past this rule is worse than one reported twice.

Module '@/widgets/db-connection' directly imports '@/internal/DBConnection/secret'.
```ts
import { secret } from '@/internal/DBConnection/secret';
```
FIX: Move what you need out of @/internal, or expose it through @/config.
[Error] ─────────────────────────────────────────
[exit] Found 4 problems, none auto-fixable.
   Run `deslop baseline` to silence all 4 problems.
